As each function, procedure, variable is created → it’s explained here.

AI does most of the heavy lifting.

Contains rules for global/private, security implementations, what mustn’t be touched etc. Gives all functions context to remove guessing, stay on target and know exactly what is at disposal (and how adding or changing things will cause cascading effects)

A wikipedia for the project.

Note, this is only populated as code is written. It is not a “plan” that is generated at the start. This is the real stuff that already exists.